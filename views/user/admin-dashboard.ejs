<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <title>⚠️Admin| Details</title>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-4">
          <img
            src="../images/logo.png"
            alt="NotesPedia Logo"
            class="img-fluid"
          />
          <a href="/logout"
            ><button class="btn btn-danger offset-5">Logout</button></a
          >
        </div>

        <div class="col-8 mt-5">
          <div class="card mt-5">
            <div class="card-header">NotesPedia</div>
            <div class="card-body">
              <h5 class="card-title">Details about Uploaded Notes</h5>
              <p class="card-text">
                Notes Uploaded Till now:
                <span class="text-danger"><b><%= totalDoc %></b></span>
              </p>
            </div>
          </div>

          <div class="card mt-3">
            <div class="card-header">Notes Details</div>
            <div class="card-body">
              <h5 class="card-title">List of Uploaded Notes</h5>
              <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Title</th>
                    <th scope="col">Owner</th>
                    <th scope="col">Description</th>
                  </tr>
                </thead>
                <tbody>
                  <% for (let i = 0; i < fetchDoc.length; i++) { %>
                  <tr>
                    <th scope="row"><%= i + 1 %></th>
                    <td><%= fetchDoc[i].title %></td>
                    <td><%= fetchDoc[i].owner %></td>
                    <td><%= fetchDoc[i].desc %></td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
